let add = (num1 , num2 )=>
{
    return num1+num2 
}

let result = add(10,20)
console.log(result)

// if arrow functions are one liner no need to use curly braces and return statemenrt
//  let add = (num1 + num2 ) => num1+num2   works same as the above function 